<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Edit Post</title>
  <link rel="stylesheet" href="/edit.css">
</head>


<body>
  <div class="form-wrapper">
    <h2>Edit Your Post</h2>

    <div class="field">
      <label>Post ID:</label>
      <div class="readonly-field"><%= post.id %></div>
    </div>

    <div class="field">
      <label>Username:</label>
      <div class="readonly-field"><%= post.username %></div>
    </div>

    <form method="POST" action="/posts/<%= post.id %>?_method=PATCH">
      <div class="field">
        <label for="content">Edit Content:</label>
        <textarea id="content" name="content" rows="8" placeholder="Update your post..."><%= post.content %></textarea>
      </div>
      <button type="submit">Update Post</button>
    </form>
  </div>
</body>
</html>
